const initState = {
    // ip: "http://localhost",
    // port: ":9000",
    ip: "",
    port: "",
    storeIp: "http://localhost",
    storePort: ":9000",
}

const reducer = (state = initState) => {
    return state;
}

export default reducer;